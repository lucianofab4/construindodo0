# Copie este arquivo para .env e preencha os valores
DATABASE_URL=postgresql://postgres:sua_senha@localhost:5432/lucianodias_db

# CORS: URL do frontend no Netlify (ou localhost para dev)
FRONTEND_URL=http://localhost:3000

# ── Email (formulário de contato → luciano.dias@nacionalsign.com) ──────────
# OPÇÃO 1 — Gmail / Google Workspace
#   Gere uma "Senha de App" em: https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=luciano.dias@nacionalsign.com
SMTP_PASS=sua_senha_de_app_aqui
#
# OPÇÃO 2 — Outro provedor (Outlook, Zoho, etc.)
# SMTP_HOST=smtp.office365.com
# SMTP_PORT=587
# SMTP_USER=luciano.dias@nacionalsign.com
# SMTP_PASS=sua_senha
